import { useState } from 'react'; import { LockKeyhole } from 'lucide-react'; import { Header } from '../components/Header'; import { api } from '../services/api'
export function LoginPage({ onLogin }: { onLogin: () => void }) { const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); async function submit(e: React.FormEvent) { e.preventDefault(); setError(''); setBusy(true); try { await api.login({ username, password }); onLogin() } catch (err) { setError(err instanceof Error ? err.message : 'Unable to sign in.') } finally { setBusy(false) } } return <><Header/><main className="mx-auto max-w-md px-4 py-8 sm:py-12"><section className="border border-slate-200 bg-white p-5 sm:p-7"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center bg-university-50 text-university-700"><LockKeyhole size={22}/></div><div><h2 className="font-bold text-slate-900">Administrator Login</h2><p className="text-sm text-slate-600">Sign in to manage anonymous feedback.</p></div></div><form onSubmit={submit} className="mt-7 space-y-5"><div><label className="label" htmlFor="username">Email/Username</label><input className="input" id="username" autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} required/></div><div><label className="label" htmlFor="password">Password</label><input className="input" id="password" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><div className="min-h-5" aria-live="polite">{error && <p className="field-error">{error}</p>}</div><button className="btn-primary w-full" disabled={busy}>{busy ? 'Signing in...' : 'Sign in'}</button></form></section></main></> }
