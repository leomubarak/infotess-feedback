import { BarChart, Bar, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
export function Chart({ title, data, line = false }: { title: string; data: { name?: string; date?: string; value: number }[]; line?: boolean }) { const label = line ? 'date' : 'name'; return <section className="border border-slate-200 bg-white p-4"><h3 className="mb-4 text-sm font-bold text-slate-800">{title}</h3><div className="h-64" aria-label={title}>{data.length ? <ResponsiveContainer width="100%" height="100%">{line ? <LineChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey={label}/><YAxis allowDecimals={false}/><Tooltip/><Line type="monotone" dataKey="value" stroke="#16635e" strokeWidth={2}/></LineChart> : <BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey={label} interval={0} angle={data.length > 4 ? -25 : 0} textAnchor={data.length > 4 ? 'end' : 'middle'} height={data.length > 4 ? 70 : 30}/><YAxis allowDecimals={false}/><Tooltip/><Legend/><Bar dataKey="value" name="Feedback" fill="#16635e"/></BarChart>}</ResponsiveContainer> : <p className="grid h-full place-items-center text-sm text-slate-500">No feedback data available.</p>}</div></section> }
